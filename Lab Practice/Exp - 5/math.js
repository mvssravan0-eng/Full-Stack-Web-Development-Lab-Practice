const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const modulus = (a, b) => a % b;
const exponention = (a, b) => a ** b;

// A function to find the square of a number
const square = (num) => num * num;

module.exports = {
  add,
  subtract,
  multiply,
  divide,
  modulus,
  exponention,
  square
};